# Correção mínima — desempenho da Visão Geral

Esta versão foi reconstruída diretamente sobre o ZIP enviado pelo usuário.

Alterações aplicadas somente:

1. substituição de `public/favicon.ico` pelo ícone institucional;
2. `Carregando snapshot…` → `Carregando dados…`;
3. `Snapshot demonstrativo` → `Dados operacionais`;
4. remoção apenas do metadado `.lovable` e do aviso documental em `AGENTS.md`.

Não foram alterados:

- `vite.config.ts`;
- `package.json`;
- `bun.lock`;
- plugins do Vite/TanStack;
- bootstrap e SSR;
- consultas e serviços da Visão Geral;
- componentes, estados de loading ou chamadas de API;
- relatórios, download e exclusão.
